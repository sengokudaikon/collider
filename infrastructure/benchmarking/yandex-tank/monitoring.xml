<?xml version="1.0"?>
<Monitoring>
    <Host address="localhost">
        <CPU measure="user,system,iowait,idle" />
        <System measure="la1,la5,la15,csw,int" />
        <Memory measure="free,used,cached,buff" />
        <Disk measure="read,write" devices="sda1,sda2" />
        <Net measure="recv,send" />
    </Host>
    
    <!-- Custom metrics for application monitoring -->
    <Custom>
        <Metric name="collider_events_per_second" 
                cmd="curl -s http://localhost:8080/metrics | grep events_processed_total | tail -1 | awk '{print $2}'" 
                diff="1" />
        <Metric name="collider_database_connections" 
                cmd="curl -s http://localhost:8080/metrics | grep db_connections_active | tail -1 | awk '{print $2}'" />
        <Metric name="collider_memory_usage_bytes" 
                cmd="curl -s http://localhost:8080/metrics | grep memory_usage_bytes | tail -1 | awk '{print $2}'" />
    </Custom>
</Monitoring>